import styled from 'styled-components';

import { Input as InputUnform } from '@rocketseat/unform';

export const InputStyled = styled(InputUnform)`
  background: rgba(0, 0, 0, 0.04);
  border: 0;
  height: 40px;
  padding: 8px;
  border-radius: 4px;
`;
